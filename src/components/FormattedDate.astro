---
interface Props {
  date: Date;
  lang: string;
}

const { date, lang } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString(lang, {
      month: 'short',
      day: 'numeric',
      year: 'numeric',
    })
  }
</time>
